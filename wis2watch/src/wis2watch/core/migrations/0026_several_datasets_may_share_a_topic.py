# Generated by Django 5.2.7 on 2026-08-27 17:44

"""Stop the topic keying a dataset, and let the centre and identifier do it.

A centre routinely publishes several datasets on one topic -- a wis2box makes
one per station group and every one of them lands on the centre's single
surface-based-observations topic, and one dj-anm topic carries a METAR record
and a SPECI record both. The unique constraint made that a catalogue error, so
every sync was refusing the second record and the ones behind it: nine of the
region's sixty-three, silently, on every run for days.

Nothing is dropped or merged. The index on the topic stays, since resolution
still reads it; only its uniqueness goes. The identifier stops being unique
across the region and becomes unique within a centre, which is the grain the
catalogue actually publishes at.
"""

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('wis2watchcore', '0025_a_centre_announcing_its_record_is_not_publishing'),
    ]

    operations = [
        migrations.AlterField(
            model_name='dataset',
            name='identifier',
            field=models.CharField(help_text='URN identifier of the dataset', max_length=500),
        ),
        migrations.AlterField(
            model_name='dataset',
            name='wmo_topic_hierarchy',
            field=models.CharField(help_text='MQTT topic hierarchy for this dataset', max_length=500),
        ),
        migrations.AddConstraint(
            model_name='dataset',
            constraint=models.UniqueConstraint(fields=('node', 'identifier'), name='unique_dataset_identifier_per_node'),
        ),
    ]
